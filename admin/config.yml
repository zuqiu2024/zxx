backend:
  name: git-gateway
  branch: main

collections:
  - name: "blog"
    label: "Blog"
    folder: "src/posts"
    create: true
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}
